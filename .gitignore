# Node modules
node_modules/

# Environment files
.env
.env.*.local

# Vite output
dist/

# Logs (single rule covers all — npm/yarn/pnpm debug + runtime + temp logs)
*.log

# Editor directories
.vscode/
.idea/
.DS_Store

# Build cache
.cache/

# System files
Thumbs.db
.vercel

# مجلد مخزن pnpm المحلي (اسمه الحرفي على القرص "%LOCALAPPDATA%") — يجب ألا يُرفع
%LOCALAPPDATA%/

# Temporary build/audit artifacts
*.log
tsc_*.txt
typecheck_result.txt
compile*.txt
build_output.txt
check.txt
dbquery*_temp.mjs

# النسخ الاحتياطية — لا تُرفع أبداً
backups/

# عروض العمل التصميمية — أصول محلية غير خاضعة للإصدار
docs/*.pptx
