# 10 — محرك سير العمل

workflow_definitions (versioned) → instances → transitions_log.
كل انتقال: CurrentState→Action→Actor→Condition→NextState→Authorization→Timestamp→Reason.
SLA: duration, escalation, pause/resume, breach.