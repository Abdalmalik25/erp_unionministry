# 23 — هجرة البيانات

Pipeline: Extract → Profile → Normalize → Map → Validate → Deduplicate → Preview → Human Approval → Import → Reconcile → Audit.
يحفظ Original Source/Value/Mapped/Transformation/Batch.